const column = document.querySelector(".column");

const addTask = (event) => {
    console.log(event);
    event.preventDefault();
}
for (let i = 0; i < column.length; i++) {
    console.log(column)
}